[project]
name = "gunicorn-reload-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.8",
    "gunicorn>=23.0.0",
    "requests>=2.32.5",
    "uvicorn>=0.39.0",
]
